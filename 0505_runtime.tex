\subsection{Runtime analysis}
In this last section, 
- we analyze the runtime when varying n and k.
- for n, se sub sample from the same dataset of the previous section and analyze the sam four algorithms, shifted schore matching has the same runtime of simple scorematching
